<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- CSV экспорт из GraphActivity. -->
    <cache-path name="shared_files" path="." />
    <!-- Записанные raw-сессии (SessionRecorder). -->
    <external-cache-path name="sessions" path="sessions/" />
    <!-- crash_log.txt для «отправить отчёт» (CrashReporter, filesDir). -->
    <files-path name="crash" path="." />
</paths>
